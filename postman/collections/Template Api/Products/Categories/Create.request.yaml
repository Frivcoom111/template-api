$kind: http-request
name: Create
order: 1000
url: '{{baseURL}}/categories'
method: POST
headers:
  - key: Content-Type
    value: application/json
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{adminToken}}'
body:
  type: json
  content: |-
    {
      "name": "Guitarra Eletrica"
    }
